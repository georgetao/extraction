In directory external to project directory:

	python3 -m venv env; source env/bin/activate

Navigate to project directory and install dependencies
	cd classifier_flask_app; pip install  -r requirements.txt

Run Application
	python main.py

Look at http://localhost:8080 in browser